package util;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

public class DateValidator {
	public static boolean validateDateToRace(String date, DateTimeFormatter format) {
		LocalDateTime now = LocalDateTime.now();
		LocalDateTime testDate = LocalDateTime.parse(date,format);
		
		if(testDate.isBefore(now)) {
			return false;
		}else if(testDate.isEqual(now)){
			return false;
		}else {
			return false;
		}	
	}
	
	public boolean validateDateBirth(String dateBirth, DateTimeFormatter format) {
		LocalDateTime now = LocalDateTime.now();
		LocalDateTime testDate = LocalDateTime.parse(dateBirth,format);
		
		if(testDate.isAfter(now)) {
			return true;
		}else if(testDate.isEqual(now)){
			return true;
		}else {
			return false;
		}	
	}
	
	
}
